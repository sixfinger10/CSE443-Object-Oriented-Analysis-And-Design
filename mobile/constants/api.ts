import Constants from "expo-constants";

/**
 * CHANGE THIS when your desktop IP changes
 * Must be your computer's LAN IP (same Wi-Fi as phone)
 */
const LAN_IP = "192.168.1.108";

export const API_BASE = `http://${LAN_IP}:8080`;

